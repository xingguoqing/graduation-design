server:
    port: 8091
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/


spring:
    http:
        encoding: UTF-8
        enabled: true
    messages:
        encoding: UTF-8
    application:
        name: back-manage
    datasource:
        url: jdbc:mysql://127.0.0.1:3306/cuicui?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
        username: root
        password: 123456
        driverClassName: com.mysql.jdbc.Driver

mybatis:
    mapperLocations: classpath:mapper/*.xml

banner:
    charset:   UTF-8

